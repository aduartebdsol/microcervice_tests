language: node_js
node_js:
  - "8.2.1"

# install:
#   - npm install newman

before_script:
  # - node --version
  # - npm --version
  # - node_modules/.bin/newman --version
  - sudo apt-get update
  - sudo apt-get --allow-unauthenticated install openvpn
script:
  # - node_modules/.bin/newman run BDS_microserviceAutom.json
  - sudo openvpn #--config #HIT-UDP4-10195-config.ovpn #--user Aduarte --http-proxy s p up
# notifications:
#   slack:
#     rooms:
#       - bdsar:0M9kV1Khc0s450dRrAB8YrMO#notf_bot_microserv
#     on_success: change
#     on_failure: change
